<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.project1">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_pokeball"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_pokeball"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project1">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".MainActivity" android:label="@string/app_name" android:theme="@style/Theme.Project1"/>
    </application>

</manifest>